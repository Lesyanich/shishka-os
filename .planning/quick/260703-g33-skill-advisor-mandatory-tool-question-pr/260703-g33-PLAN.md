---
phase: quick-260703-g33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/constitution/operational-rules.md
  - docs/operations/skill-advisor.md
autonomous: true
requirements: [SKILL-ADVISOR-TOOL-QUESTION, SKILL-ADVISOR-RESEARCH-REGISTRY]

must_haves:
  truths:
    - "When the CEO asks which skill/command/tool to use, the rule mandates a full registry read + arsenal + skills-lock source check + find-skills fallback before claiming no tool exists"
    - "When the CEO asks to research open source (or the agent does on its own initiative), the rule mandates ending with a registry update (install + skill-advisor section + ADVISOR-MAP row, or a recorded negative result)"
    - "The find-skills ADVISOR-MAP row substring-matches common tool-question phrasings like 'какую команду', 'which skill', 'что использовать'"
    - "The Meta/discovery find-skills bullet references the new protocol"
    - "Both the existing Origin note and the new Origin 2 note are present in RULE-SKILL-ADVISOR"
  artifacts:
    - path: "docs/constitution/operational-rules.md"
      provides: "RULE-SKILL-ADVISOR with Tool-question protocol + Research to registry subsections and Origin 2 line"
      contains: "Tool-question protocol"
    - path: "docs/operations/skill-advisor.md"
      provides: "Extended find-skills ADVISOR-MAP keywords + protocol note in Meta/discovery section"
      contains: "какую команду"
  key_links:
    - from: "docs/operations/skill-advisor.md find-skills bullet"
      to: "operational-rules.md RULE-SKILL-ADVISOR"
      via: "cross-reference in prose"
      pattern: "RULE-SKILL-ADVISOR"
---

<objective>
Harden the Skill Advisor against the 2026-07-03 incident: the agent keyword-grepped the
registry, missed relevant tools, and told the CEO "no ready-made command exists" (wrong).
Separately, when asked to research open source, the agent researched but never updated the
registry with findings.

Two docs-only changes encode the CEO directive:
1. A hard tool-question protocol in RULE-SKILL-ADVISOR — full registry read + arsenal +
   partially-installed-collection check + find-skills fallback before ever claiming "no tool".
2. A hard research-to-registry rule — open-source research must end with a registry update
   (install + skill-advisor section + ADVISOR-MAP row) or a recorded negative result.
3. Fix the find-skills ADVISOR-MAP row so common phrasings substring-match (the incident
   trigger "какую команду или скилл" did not match "какой скилл" / "какой инструмент").

Purpose: never again mis-answer a "which tool?" question or lose research findings.
Output: edited operational-rules.md and skill-advisor.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/constitution/operational-rules.md
@docs/operations/skill-advisor.md

<interfaces>
Exact anchors the executor edits against. Both files already read during planning; no
codebase exploration needed.

operational-rules.md RULE-SKILL-ADVISOR ends at ~line 515-518 with:

  **Compound engineering.** When a tool would have helped but no hint fired, add a row to the
  `ADVISOR-MAP` table (bilingual RU+EN keywords) — per `RULE-COMPOUND-ENGINEERING`.

  > Origin: 2026-06-28. CEO: «я не могу запомнить все полезные фичи — хочу чтобы Клод сам подсказывал какими скиллами упростить работу».

The two new subsections MUST be inserted BETWEEN the "Compound engineering." paragraph and
the existing "> Origin: 2026-06-28" line. Keep that origin line intact; add "> Origin 2:"
immediately after it.

skill-advisor.md find-skills ADVISOR-MAP row (currently line 145):

  | найди скилл,какой скилл,is there a skill,find a skill,какой инструмент,какие инструменты | `find-skills` | discover installable skills |

skill-advisor.md Meta / discovery section (lines 102-106), find-skills bullet (line 104):

  - **`find-skills`** — discover installable skills for a capability gap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Tool-question protocol + Research-to-registry subsections to RULE-SKILL-ADVISOR</name>
  <files>docs/constitution/operational-rules.md</files>
  <action>
In section RULE-SKILL-ADVISOR, insert two new bold-lead-in subsections BETWEEN the
"**Compound engineering.**" paragraph (ends ~line 516) and the existing "> Origin: 2026-06-28"
line (line 518). Do NOT remove or alter the existing Origin line — it stays. Match the
section's existing style: bold lead-in, terse bullets, English only (Russian only inside the
verbatim quotes). Insert exactly these two subsections:

**Tool-question protocol (hard).** When the CEO directly asks which skill/command/tool to use
for a task ("какой скилл/команду использовать", "какую команду или скилл", "which skill should
I use", etc.), do this **in order before answering** — never keyword-grep the registry and
guess:
- Read `docs/operations/skill-advisor.md` **in full** — every domain section, not a grep.
- Check the installed arsenal: skills listed in session context, slash-commands, MCP servers.
- Check `skills-lock.json` sources for **partially-installed collections** — the source repo
  may hold uninstalled skills that fit (e.g. `petrkindlmann/qa-skills` ships 50, only 6
  installed; the answer to a gap may be an already-vetted-but-uninstalled skill).
- If still no fit, run `find-skills` to search installable skills.
- Only after all four steps may you say "no ready-made tool exists".

**Research to registry (hard).** When the CEO asks to research open source (or you research it
on your own initiative) for a capability gap, the deliverable is **not** a chat answer — it
MUST end with a registry update:
- Install the chosen skill(s) (update `skills-lock.json`).
- Add the skill to the matching domain section of `skill-advisor.md`.
- Add a bilingual (RU+EN) row to `ADVISOR-MAP`.
- If nothing was worth installing, record the negative result (what was searched, when, why
  rejected) so the search is not blindly repeated.
- Research that ends with only a recommendation in chat is an **incomplete task**.

Then, immediately AFTER the existing "> Origin: 2026-06-28..." line, add a new line (verbatim
Russian quote is an established convention for Origin lines in this file):

> Origin 2: 2026-07-03. CEO: «когда я задаю вопрос какой скилл/команду использовать — сначала посмотри все доступные варианты; если прошу поискать опен сорс — не просто ищи, а обновляй наш реестр».
  </action>
  <verify>
    <automated>cd /Users/lesianich/code/shishka/.claude/worktrees/nifty-nightingale-3df0be && grep -q "Tool-question protocol (hard)" docs/constitution/operational-rules.md && grep -q "Research to registry (hard)" docs/constitution/operational-rules.md && grep -q "Origin 2: 2026-07-03" docs/constitution/operational-rules.md && grep -q "Origin: 2026-06-28" docs/constitution/operational-rules.md && grep -q "skills-lock.json" docs/constitution/operational-rules.md && echo PASS</automated>
  </verify>
  <done>RULE-SKILL-ADVISOR contains both new subsections, the four ordered steps, the research-to-registry mandate, and both the original Origin and the new Origin 2 lines. The "Compound engineering." paragraph and existing Origin line are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix find-skills ADVISOR-MAP triggering + link Meta/discovery bullet to the protocol</name>
  <files>docs/operations/skill-advisor.md</files>
  <action>
Two edits, both to the find-skills entry.

Edit A — ADVISOR-MAP row (line 145). Extend the keyword list so common tool-question phrasings
substring-match. The parser does plain lowercase substring matching. Keep keywords lowercase,
comma-separated, no spaces around commas. ADD these keywords to the existing list (do NOT
remove existing ones): `какую команду`, `какой команд`, `что использовать`, `чем протестир`,
`which skill`, `which command`, `what tool`, `какой тул`. Do NOT add bare "скилл" or "команда"
(false-positive-prone). Resulting row:

  | найди скилл,какой скилл,какую команду,какой команд,что использовать,чем протестир,какой инструмент,какие инструменты,какой тул,is there a skill,find a skill,which skill,which command,what tool | `find-skills` | discover installable skills |

Edit B — Meta / discovery section (line 104). Extend the find-skills bullet to mention the new
protocol and cross-reference the rule. Replace:

  - **`find-skills`** — discover installable skills for a capability gap.

with:

  - **`find-skills`** — discover installable skills for a capability gap. A direct tool-question
    ("which skill/command should I use?") triggers a **full-registry read + `skills-lock.json`
    source check** before answering; open-source research must **end with a registry update**
    (install + this file + ADVISOR-MAP row). See `operational-rules.md` § RULE-SKILL-ADVISOR.
  </action>
  <verify>
    <automated>cd /Users/lesianich/code/shishka/.claude/worktrees/nifty-nightingale-3df0be && grep -q "какую команду" docs/operations/skill-advisor.md && grep -q "which skill" docs/operations/skill-advisor.md && grep -q "what tool" docs/operations/skill-advisor.md && grep -q "RULE-SKILL-ADVISOR" docs/operations/skill-advisor.md && grep -q "full-registry read" docs/operations/skill-advisor.md && ! grep -qE "^\| .*,команда," docs/operations/skill-advisor.md && echo PASS</automated>
  </verify>
  <done>The find-skills ADVISOR-MAP row includes the new bilingual keywords (без bare "скилл"/"команда"), the Meta/discovery bullet references the tool-question protocol + skills-lock check + registry-update mandate and cross-links RULE-SKILL-ADVISOR.</done>
</task>

</tasks>

<verification>
- `grep -q "Tool-question protocol (hard)" docs/constitution/operational-rules.md` → match
- `grep -q "Research to registry (hard)" docs/constitution/operational-rules.md` → match
- `grep -q "Origin 2: 2026-07-03" docs/constitution/operational-rules.md` → match
- Existing `> Origin: 2026-06-28` line still present (not overwritten)
- `grep -q "какую команду" docs/operations/skill-advisor.md` → match
- `grep -q "which skill" docs/operations/skill-advisor.md` → match
- find-skills Meta/discovery bullet cross-references RULE-SKILL-ADVISOR
- No new npm deps, no script edits, English-only prose (Russian confined to Origin quotes + ADVISOR-MAP keywords)
</verification>

<success_criteria>
- CEO directive encoded as two hard subsections in RULE-SKILL-ADVISOR (tool-question 4-step protocol; research-to-registry mandate) with Origin 2 provenance line.
- The incident phrasing ("какую команду или скилл") now substring-matches the find-skills ADVISOR-MAP row.
- Meta/discovery find-skills bullet points readers to the full protocol.
- Docs-only change: only operational-rules.md and skill-advisor.md touched; no scripts, no code, no new deps.
</success_criteria>

<output>
After completion, create `.planning/quick/260703-g33-skill-advisor-mandatory-tool-question-pr/260703-g33-SUMMARY.md`
</output>
