{
  "installCommand": "npm install --force && (cd ../../packages/contracts && npm install --force)",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
