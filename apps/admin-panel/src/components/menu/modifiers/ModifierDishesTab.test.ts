import { describe, it, expect } from 'vitest'
import { ModifierDishesTab } from './ModifierDishesTab'

describe('ModifierDishesTab', () => {
  it('is exported as a function component', () => {
    expect(typeof ModifierDishesTab).toBe('function')
  })
})
