import { describe, it, expect } from 'vitest'
import { DishGroupAttachEditor } from './DishGroupAttachEditor'

describe('DishGroupAttachEditor', () => {
  it('is exported as a function component', () => {
    expect(typeof DishGroupAttachEditor).toBe('function')
  })
})
