import { describe, it, expect } from 'vitest'
import { ModifierGroupsTab } from './ModifierGroupsTab'

describe('ModifierGroupsTab', () => {
  it('is exported as a function component', () => {
    expect(typeof ModifierGroupsTab).toBe('function')
  })
})
