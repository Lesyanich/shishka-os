import { describe, it, expect } from 'vitest'
import { GroupOptionEditor } from './GroupOptionEditor'

describe('GroupOptionEditor', () => {
  it('is exported as a function component', () => {
    expect(typeof GroupOptionEditor).toBe('function')
  })
})
