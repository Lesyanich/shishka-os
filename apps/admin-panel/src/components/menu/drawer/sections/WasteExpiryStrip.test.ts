import { describe, it, expect } from 'vitest'
import { WasteExpiryStrip } from './WasteExpiryStrip'

describe('WasteExpiryStrip', () => {
  it('exports the WasteExpiryStrip component', () => {
    expect(typeof WasteExpiryStrip).toBe('function')
  })
})
