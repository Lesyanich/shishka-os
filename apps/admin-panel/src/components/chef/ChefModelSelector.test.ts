import { describe, expect, it } from 'vitest'
import { ChefModelSelector } from './ChefModelSelector'

describe('ChefModelSelector', () => {
  it('exports the component', () => {
    expect(typeof ChefModelSelector).toBe('function')
  })
})
