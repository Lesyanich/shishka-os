import { describe, it, expect } from 'vitest'
import { StationCountPage } from './StationCountPage'

describe('StationCountPage', () => {
  it('exports StationCountPage component', () => {
    expect(typeof StationCountPage).toBe('function')
  })
})
