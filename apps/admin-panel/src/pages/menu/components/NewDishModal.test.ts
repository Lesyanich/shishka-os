import { describe, expect, it } from 'vitest'
import { NewDishModal } from './NewDishModal'

describe('NewDishModal', () => {
  it('exports the component', () => {
    expect(typeof NewDishModal).toBe('function')
  })
})
