import { describe, expect, it } from 'vitest'
import { BomTreeEditor } from './BomTreeEditor'

describe('BomTreeEditor', () => {
  it('exports the component', () => {
    expect(typeof BomTreeEditor).toBe('function')
  })
})
