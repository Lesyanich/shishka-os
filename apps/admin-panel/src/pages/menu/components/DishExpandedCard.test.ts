import { describe, expect, it } from 'vitest'
import { DishExpandedCard } from './DishExpandedCard'

describe('DishExpandedCard', () => {
  it('exports the component', () => {
    expect(typeof DishExpandedCard).toBe('function')
  })
})
