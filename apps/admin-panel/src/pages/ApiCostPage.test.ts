import { describe, it, expect } from 'vitest'
import { ApiCostPage } from './ApiCostPage'

describe('ApiCostPage', () => {
  it('exports ApiCostPage component', () => {
    expect(typeof ApiCostPage).toBe('function')
  })
})
