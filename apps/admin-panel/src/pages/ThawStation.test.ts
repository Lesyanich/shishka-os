import { describe, it, expect } from 'vitest'
import { ThawStation } from './ThawStation'

describe('ThawStation', () => {
  it('exports ThawStation component', () => {
    expect(typeof ThawStation).toBe('function')
  })
})
