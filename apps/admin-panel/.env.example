# LightRAG (Brain View)
# Default points at the local Phase 1 server. Override for staging/prod.
VITE_LIGHTRAG_BASE_URL=http://localhost:9621
# Optional. Empty for local dev — server runs without auth.
VITE_LIGHTRAG_API_KEY=
